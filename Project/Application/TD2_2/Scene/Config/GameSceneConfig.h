#pragma once
#include "MathCore.h"

namespace GameSceneConfig {
const Vector2 kFrameSize = { 2.0f, 2.0f };
const Vector2 kStageSize = { kFrameSize.x * 28.0f,  kFrameSize.y * 28.0f };
const Vector2 kStageCenter = { 0.0f,0.0f };
}